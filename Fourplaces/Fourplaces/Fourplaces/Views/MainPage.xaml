<?xml version="1.0" encoding="utf-8" ?>
<mvvm:BaseContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Fourplaces.Views.MainPage"
             xmlns:mvvm="clr-namespace:Storm.Mvvm.Forms;assembly=Storm.Mvvm.Forms"
             x:Name="mainpage">

    <!--<ListView ItemsSource="{Binding TaskItems}">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout Margin="8">

                        <Label Text="{Binding Nom}"
                           FontAttributes="Bold" />

                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>-->

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="{Binding ToolBarText1}" Command="{Binding Cmd1}"  Order="Primary" Priority="0"/>
        <ToolbarItem Text="{Binding ToolBarText2}" Command="{Binding Cmd2}"  Order="Primary" Priority="1" />
        <ToolbarItem Text="{Binding ToolBarText3}" Command="{Binding Cmd3}"  Order="Primary" Priority="2" />
    </ContentPage.ToolbarItems>

    <!--<ContentPage.ToolbarItems IsVisible="{Binding IsConnected}">
        <ToolbarItem Name="My account" Command="{Binding MyAccount}"  Order="Primary" Priority="0" />
        <ToolbarItem Name="Add place" Command="{Binding AddPlace}"  Order="Primary" Priority="1" />
    </ContentPage.ToolbarItems>-->

    <ListView ItemsSource="{Binding ListPlaceSummary}"
                  HasUnevenRows="true"
                  SelectedItem="{Binding PlaceSummary}">

        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout Orientation="Horizontal">
                        <Image Source="{Binding SourceImage}" WidthRequest="90" HeightRequest="90"/>
                        <StackLayout Orientation="Vertical">
                            <Label Text="{Binding Title}" FontAttributes="Bold"/>
                            <Label Text="{Binding Description  , StringFormat='Description : {0}'}" LineBreakMode="TailTruncation" MaxLines="3"/>
                        </StackLayout>
                    </StackLayout>
                </ViewCell>

            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>

</mvvm:BaseContentPage>    
    
    
<!--<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Fourplaces"
             x:Class="Fourplaces.Views.MainPage">

    <ListView ItemsSource="{Binding TaskItems}">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout Margin="8">

                        <Label Text="{Binding Nom}"
                           FontAttributes="Bold" />

                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>

</ContentPage>-->